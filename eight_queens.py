class EightQueens:

      def print_name(self, first_name: str, last_name: str) -> str:
            """
            function that prints your first and last name
            """
        return f"Hi, my name is {first_name} {last_name}"

      def add_two_numbers(self, number_one: int, number_two: int) -> int:
            """
            function that adds two numbers together
            """
        return number_one + number_two

eq = EightQueens()
print(eq.print_name("nick", "bermingham"))

#adding in some comments
